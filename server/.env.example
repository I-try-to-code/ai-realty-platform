# Database configuration for local PostgreSQL
# Replace 'YOUR_PASSWORD' with your actual local PostgreSQL password, and adjust host/port if needed!
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/ai_realty?schema=public"

# JWT configuration for user login sessions
JWT_SECRET="ai_realty_secret_session_token_key_12345"

# Server Port
PORT=5000
